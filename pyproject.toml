[project]
name = "faang-pulse-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gradio >= 6.8.0",
    "matplotlib >= 3.10.8",
    "yfinance==1.0",
    "scikit-learn>=1.0.0",
    "skops >= 0.13.0"
]

[tool.setuptools.packages.find]
exclude = ["audits*", "support*"]

